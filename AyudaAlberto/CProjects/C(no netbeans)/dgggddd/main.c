#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>
#include <string.h>
#define FALSE 0
#define TRUE 1
#define MAX_CONTACTOS 20

int main(int argc, char *argv[]) {
 	int i;
 	char *contrasena="O?rrv/qc";
 	//int tamanho = strlen(contrasena);
 	int caracter;
 	char cadena[strlen(contrasena) - 1] = '\0';
 	//char desencriptada[tamanho];
 	printf("%s",cadena);
    //for(i=0;i<=8;i++){
    //	caracter = contrasena[i];
    //	desencriptada[i] = caracter+1;
	//}
	//printf("%s",desencriptada);
	return 0;
}
